##HTML Link PARSER

A html link parser to extract all the links in a HTML document 
you can provide html document with the help of -file=example.html 
and return a list of all the links and text inside the links 
